{
  "name": "reponsive-images",
  "version": "0.1.0",
  "scripts": {
    "serve": "browser-sync start -s -f . --no-notify -b chrome.exe --port 3000"
  },
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-contrib-clean": "~0.6.0",
    "grunt-mkdir": "~0.1.2",
    "grunt-responsive-images": "^0.1.6"
  }
}
